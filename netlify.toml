[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — Vue Router uses HTML5 history mode, so every path must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
