# خطة التكامل الذكيّ للقطاعات (حيّة) — القطاع مكوّنٌ عابر في الإضافة · العرض · الوصول · البحث

> **الهدف:** جعل **نطاق النشاطات/القطاعات** (`sectors.ts` — 21 قطاعًا) **مكوّنًا عابرًا (cross-cutting)** حقيقيًّا في كل أسطح المنصّة: كمُدخل عند الإضافة، ومخرَج عند العرض، وخيار عند الوصول/الفلترة، ومُرجِّح عند البحث والتوصية — بحيث يعود **سياق قطاعات المستخدم** ليخصّص تجربته في كل مكان.
> **المرجع:** `DOC/TAXONOMY_PLAN.md` (نموذج التصنيف) + هذه الخطة (طبقة الاستهلاك والتخصيص).

---

## 1. المشكلة (تدقيق مُثبَت)

التصنيف مُدمج **جزئيًّا**: القطاع مكوّن في الإضافة/العرض/البحث عبر عمل B1–B4، **لكن تفضيل المستخدم لا يصل إليه** — الحلقة مقطوعة.

| البُعد | القطاع مكوّن؟ | تفضيل المستخدم يصل؟ | الفجوة |
|---|---|---|---|
| الإضافة (`CreateOpportunity`/`Profile`) | ✅ | ❌ | لا يُبذَر القطاع الافتراضيّ من اهتمام المستخدم |
| العرض/الفلترة (`Opportunities`/`Requests`/`Interviewers`) | ✅ `TaxonomyTree` | ❌ | `treeSel = ref({})` يبدأ فارغًا |
| المطابقة (`matching.ts`/`matchProfile.ts`) | ✅ قطاع↔قطاع | ❌ | ملف الباحث يشتقّ القطاع من المهارات فقط، لا من `interestedSectors` |
| البحث (`globalSearch`) | ✅ واعٍ بالتصنيف | ❌ | `category` وسيط يدويّ لا يُبذَر ولا يُرجَّح بقطاعات المستخدم |
| التوصية (`Dashboard.recommended`) | 🟡 `matchRate` ثابت | ❌ | لا يقرأ `persona.interestedSectors` |

**الدليل القاطع:** `usePersonaStore` يُقرأ في موضعين فقط (`OnboardingPage`+`ProfilePage`)؛ و`interestedSectors` لا يُستهلَك خارج onboarding — **بيانات ميتة**.

---

## 2. المبدأ المعماريّ: طبقة سياق واحدة (لا ترقيع لكل صفحة)

**القاعدة الذهبية:** لا نبعثر منطق «قطاعات المستخدم» في كل صفحة. نبني **مصدر حقيقة واحدًا** يستهلكه الجميع:

### `useSectorContext()` — composable مركزيّ (جديد: `src/composables/useSectorContext.ts`)
يحلّ **«قطاعات المستخدم الفعّالة»** بترتيب أولويّة، ويعطي أدوات موحّدة:
```ts
interface SectorContext {
  explicit: string[]        // من persona.interestedSectors (اختيار صريح في onboarding/الإعدادات)
  derived: string[]         // مشتقّة من مهارات/خبرات الملف عبر dominantSector (احتياط)
  effective: string[]       // explicit ∪ derived (المصدر الموحّد للتخصيص)
  primary?: string          // القطاع الأبرز (لأغراض الافتراض المفرد)
  has: boolean              // هل لدى المستخدم سياق قطاعيّ أصلًا؟
  // أدوات الاستهلاك الموحّدة:
  seedTree(): { category?: string }         // بذرة فلتر TaxonomyTree (primary، قابل للتجاوز)
  boost(itemSector?: string): number        // معامل ترجيح (0..1) لعنصر حسب قربه من سياق المستخدم
  rankSearch<T>(items, getSector): T[]       // إعادة ترتيب نتائج البحث بالسياق
  matchInput(): { sector?: string }          // مُدخل seekerMatchProfile من السياق لا المهارات فقط
}
```
- يقرأ `PersonaStore.interestedSectors` (المُزامَن مع Laravel) + `ProfileStore` (المهارات) — **يتفاعل** مع تغيّرهما.
- **يحترم الدور النشط والـkind** (§5): الباحث يرى «فرصًا في قطاعاتي»، الجهة ترى «مرشّحين/طلبات في قطاعاتي»، الخبير «تكليفات في قطاعاتي» — نفس السياق، عدسة مختلفة.

---

## 3. مصفوفة السياقات الكاملة (كل سطح × الأبعاد الأربعة)

| السطح | إضافة (input) | عرض (output) | وصول (فلترة/افتراض) | بحث |
|---|---|---|---|---|
| **onboarding** | ✅ (منجز — يلتقط `interestedSectors`) | — | — | — |
| **الرئيسية/Dashboard** | — | شارة «قطاعاتي» + قسم «موصى به في قطاعاتك» | — | — |
| **الفرص `Opportunities`** | — | ترتيب/تمييز عناصر قطاعاتي | **بذر `treeSel` من `seedTree()`** + شريحة «قطاعاتي/الكل» | — |
| **الطلبات `Requests`** | — | مثله | **بذر `treeSel`** | — |
| **سوق المقيّمين/الخبراء** | — | مثله | **بذر `treeSel`** | — |
| **استكشاف الأشخاص `People`** | — | ترتيب حسب القطاع | فلتر قطاع مبذور | — |
| **إنشاء فرصة `CreateOpportunity`** | **اقتراح القطاع الافتراضيّ** من السياق (+ `autoClassify` القائم) | — | — | — |
| **الملف `Profile`/السيرة** | **بذر قطاع التفضيلات** من السياق | وسم القطاع على الملف | — | — |
| **البحث الشامل `globalSearch`** | — | — | نطاق مبذور | **ترجيح `rankSearch` بالسياق** + شريحة «ضمن قطاعاتي» |
| **المطابقة `matching`/`matchProfile`** | — | — | — | `seekerMatchProfile.sector` من `matchInput()` لا المهارات فقط |
| **الإعدادات `Settings`** | **تحرير `interestedSectors`** (إضافة/إزالة لاحقة، لا onboarding فقط) | — | — | — |

---

## 4. المراحل (مرقّمة، محكمة)

### المرحلة 1 — الأساس (طبقة السياق) ✅ **منجزة ومُتحقَّقة حيًّا**
- ✅ بناء `useSectorContext()` (`src/composables/useSectorContext.ts`) + دوال `seedTree/boost/rankSearch/matchInput`. التطبيع: codes داخليًّا (متانة عبر `getSector`)، slugs للعرض (لغة الفلاتر).
- ✅ إضافة تحرير `interestedSectors` في **مركز الإعدادات** (تبويب «الحساب» + دخوله في بحث الإعدادات) — مزامنة فوريّة عبر `PersonaStore`، لم يعد حبيس onboarding. تُحقِّق حيًّا: نقر «المالية» أضافها إلى السياق فورًا.
- ✅ 12 اختبار وحدة للـcomposable (explicit/derived/effective/union-dedupe/primary/boost/rankSearch المستقرّ/seedTree/matchInput). المجموع 244 أخضر (كان 232).
- **قرار تنفيذيّ:** تدرّج `boost` = الأبرز 1 · صريح آخر 0.8 · مشتقّ 0.4 · خارج/بلا سياق 0 (قابل للضبط في م2 عند أول مستهلك حقيقيّ).

### المرحلة 2 — الوصول والعرض (الأسواق الثلاثة) ✅ **منجزة ومُتحقَّقة حيًّا**
- ✅ شريحة «قطاعاتي ⟷ الكل» في `Opportunities`/`Requests`/`Interviewers` — بذر افتراضيّ على «قطاعاتي» عند وجود سياق، **قابل للتجاوز فورًا** بـ«الكل»، و`resetFilters` يعيده للافتراض.
- ✅ تمييز/ترتيب عناصر قطاعات المستخدم أولًا عبر `boost` كـ**كاسر تعادل** في فرز «الأعلى تطابقًا» فقط (لا يخطف فرز الراتب/الأحدث الصريح — أقلّ مفاجأة).
- ✅ حالة فارغة (بلا سياق) → الشريحة لا تظهر (`v-if="sector.has"`)، سلوك اليوم.
- **قرار تنفيذيّ (تعديل عن الخطة):** استُبدل «بذر `treeSel` المفرد» بـ**نطاق بالاتّحاد** (`inEffective` على `effective`) — أوفى بقاعدة «متعدّد القطاعات بالاتّحاد» من قطاع `primary` مفرد. `TaxonomyTree` يبقى المنتقي الدقيق (يضيّق داخل النطاق). `seedTree()`/`matchInput()` يُستهلكان في الأسطح المفردة (م4/م5).
- **أُضيف للـcomposable:** `inEffective(itemSector)` (+ اختبار) لتمركز منطق النطاق ومنع تبعثره في 3 صفحات.
- **تحقّق حيّ:** الفرص 11⟷18، المقيّمون 5⟷8 عند تبديل قطاعاتي/الكل؛ صفر أخطاء كونسول؛ 245 اختبارًا أخضر.

### المرحلة 3 — التوصية والمطابقة ✅ **منجزة ومُتحقَّقة حيًّا**
- ✅ `Dashboard.recommended`: استُبدل فرز `matchRate` الثابت بتطابق **حيّ** (`matchScore` + `seekerProfile` بسياق) + `boost` كاسر تعادل — متّسق مع الأسواق.
- ✅ حُقن `...matchInput()` في **كل** بُناة ملف الباحث الخمسة: `OpportunityCard` (نسبة كل بطاقة) · `OpportunitiesPage` · `RequestsFeedPage` · `OpportunityDetailsPage` · `RequestDetailsPage` — فالقطاع من السياق الصريح، والاشتقاق من المهارات يبقى احتياطًا (spread فارغ بلا سياق).
- **تحقّق حيّ قاطع:** نفس وظيفة Vue.js قفزت **42% → 73%** عند تبديل قطاع الاهتمام health→technology (وزن القطاع +25). صفر أخطاء كونسول · 245 اختبارًا أخضر.
- **خارج النطاق عمدًا:** `topCandidates`/`WishesPage`/`CandidateProfile` تستخدم `matchRate` مبذورًا على البيانات (عدسة الجهة تجاه المرشّح، لا المستخدم الحالي) — لا تُلمس هنا.

### المرحلة 4 — البحث ✅ **منجزة ومُتحقَّقة حيًّا**
- ✅ `globalSearch.search(..., { onlyMine })`: **ترتيب واعٍ بالقطاع دائمًا** (`rankSearch` يرفع نتائج قطاعاتي في الأقسام الثلاثة القطاعيّة requests/opportunities/interviewers، غير هادم) + معامل `onlyMine` يقيّد على الاتّحاد عبر `inEffective`.
- ✅ شريحة «ضمن قطاعاتي» في `SearchResultsPage` (تظهر مع سياق فقط) — تمرّر `onlyMine`.
- **قرار تنفيذيّ (تعديل عن الخطة):** لا **بذر-تقييد افتراضيّ** للبحث (بخلاف أسطح التصفّح) — إخفاء نتائج بحث افتراضيًّا عدائيّ للمستخدم؛ فالترتيب افتراضيّ والتقييد اختياريّ (opt-in). الأقسام غير القطاعيّة (مستخدمون/شركات/مهارات) لا يمسّها `onlyMine`.
- **تحقّق حيّ:** «ضمن قطاعاتي» قيّد الفرص 18→11 (technology) وعاد 18؛ اختبار الترتيب يؤكّد أوّل نتيجة في قطاع المستخدم. 248 اختبارًا أخضر (+3).

### المرحلة 5 — الإضافة (المُدخلات) ✅ **منجزة ومُتحقَّقة حيًّا**
- ✅ `CreateOpportunity`: حقل «المجال/القطاع» يُبذَر بتسمية القطاع الأبرز (`primary`) + تلميح «مبدئيًّا من قطاعات اهتمامك — عدّله بحرّية» (يختفي عند التعديل) + إعادة البذر عند `resetForm`. يتكامل مع `autoClassify` (يقترح مهارات/تصنيفًا من النصّ) بلا تصادم.
- ✅ `Profile`/تفضيلات الباحث: شرائح **«أضف من قطاعاتك»** أسفل «المجالات المفضّلة» — تسميات قطاعات المستخدم غير المضافة تُلحق بنقرة (اقتراح غير هادم بدل بذر صامت في حقل حرّ مُزامَن).
- **تحقّق حيّ:** منشأة بقطاع finance → الحقل مبذور «المالية والمحاسبة» + التلميح؛ باحث بـfinance+design → 3 شرائح (مع technology المشتقّة)، النقر يُلحق ويُخفي الشريحة ويُزامَن (`preferred_fields`). صفر أخطاء · 248 اختبارًا أخضر.

### المرحلة 6 — الاتّساق والتحقّق ✅ **منجزة**
- ✅ **تدقيق القراءة المباشرة:** كل قراءة لـ`interestedSectors` خارج `useSectorContext` محصورة في **أسطح الكتابة/التحرير فقط** (`PersonaStore` المصدر · `SettingsPage` المحرّر · `OnboardingPage` الالتقاط) — وهي مشروعة لأنها تعدّل الاختيار الصريح وحده (لا `effective` الذي يضمّ المشتقّ). **صفر قراءة استهلاكيّة مباشرة** في الصفحات؛ كل عرض/وصول/مطابقة/توصية/بحث/إضافة عبر الـcomposable.
- ✅ **إتمام تغطية §3:** وُصِل **`PeopleExplorer`** (قطاع الشخص مشتقّ من مهاراته عبر `dominantSector` + شريحة «قطاعاتي⟷الكل» + `boost` كاسر تعادل) — تحقّق حيّ 5⟷8.
- ✅ typecheck نظيف · 248 اختبارًا أخضر · تحقّق حيّ لكل سطح · المزامنة الخلفيّة مؤكّدة (`persona` عبر `PersonaStore` ↔ Laravel).
- **استثناءات مبرّرة (لا تُوصَل عمدًا):**
  - **`ExpertsMarketPage`**: الخبراء مُصنّفون على **محور تخصّصات الخبراء المتعامد** (mentor/coach/consultant من `personas.ts`)، لا على محور القطاعات — الوصل يخرق حوكمة «3 محاور متعامدة». (عدسة «الخبراء في قطاعاتي» يغطّيها سوق **المقيّمين** الذي له `field`→قطاع.)
  - **`topCandidates`/`WishesPage`/`CandidateProfile`**: `matchRate` مبذور على البيانات (عدسة الجهة تجاه المرشّح، لا سياق المستخدم الحالي).
  - **شارة/قسم «قطاعاتي» في Dashboard**: التوصية مُرجَّحة بالسياق فعليًّا (م3)؛ الشارة النصّيّة تحسين تجميليّ مؤجَّل بلا أثر وظيفيّ.

---

## 5. قواعد الاتّساق (البزنس واللوجك) — ملزمة

1. **البذر ليس قفلًا:** أي فلتر مبذور من السياق **قابل للتجاوز فورًا** (شريحة «الكل» + إعادة تعيين). المستخدم سيّد تجربته.
2. **متعدّد القطاعات:** `interestedSectors` قائمة — نتعامل بالاتّحاد (union) لا قطاعًا واحدًا؛ `primary` للافتراض المفرد فقط.
3. **العدسة حسب الدور النشط:** نفس السياق، مخرَج مختلف — باحث→فرص، جهة→مرشّحون/طلبات، خبير→تكليفات، مُزكٍّ→زملاء في قطاعاتي. `useSectorContext` يقرأ `authStore.role`.
4. **الـkind:** منشأة قد تُرجَّح لعدسة التوظيف افتراضيًّا، وفرد لعدسة البحث — دون حجب (سمة لا قيد).
5. **سلّم الاحتياط:** explicit → derived (مهارات) → بلا سياق (سلوك اليوم). لا فراغ يكسر الصفحة.
6. **حوكمة التصنيف:** احترام `GENERIC_BLOCKLIST` و`isGenericLabel` وإخفاء S21 (مطبّقة في `sectors.ts`) — لا نعيد فتحها.
7. **المزامنة:** كل تغيير على `interestedSectors` يُزامَن مع Laravel عبر `PersonaStore` (منجز) — فالسياق متّسق عبر الأجهزة.
8. **الأداء:** `useSectorContext` computed خفيف؛ لا استعلامات ثقيلة في كل عنصر — الترجيح دالة نقيّة.

---

## 6. الملفّات المتأثّرة (خريطة)

- **جديد:** `src/composables/useSectorContext.ts` (+ اختباره)
- **تعديل عرض/وصول:** `OpportunitiesPage` · `RequestsFeedPage` · `InterviewersMarketplacePage` · `PeopleExplorerPage`
- **تعديل توصية:** `DashboardPage` · «AI personalized top match»
- **تعديل بحث:** `globalSearch.ts` · `SearchResultsPage` · `GlobalSearchBar`
- **تعديل مطابقة:** `matchProfile.ts` (مدخل السياق)
- **تعديل إضافة:** `CreateOpportunityPage` · تفضيلات `ProfilePage`
- **تعديل إعدادات:** `SettingsPage` (تحرير `interestedSectors`)
- **بلا لمس:** `sectors.ts` (النموذج) · `PersonaStore` (المصدر، منجز) · حوكمة التصنيف

---

## 7. التحقّق (Definition of Done)

- [x] `useSectorContext` مصدر وحيد؛ صفر قراءة **استهلاكيّة** مباشرة لـ`interestedSectors` في الصفحات (القراءات المتبقّية أسطح كتابة/تحرير فقط).
- [x] كل سطح في §3 موصول ومُتحقَّق حيًّا (بذر/ترتيب/بحث/توصية/إضافة) مع تجاوز المستخدم — عدا الاستثناءات المبرّرة (م6).
- [x] اختبارات وحدة للـcomposable + للترجيح والترتيب + البحث؛ typecheck نظيف؛ **248 اختبارًا أخضر** (232 + 16 جديدة).
- [x] عدسة الدور تعمل (باحث→فرص · جهة→CreateOpportunity بذر · مقيّمون في قطاعاتي) — تحقّق حيّ.
- [x] المزامنة الخلفيّة مؤكَّدة (persona ↔ Laravel عبر `PersonaStore`).

---

*الحالة: **الخطة مكتملة — المراحل 1–6 منجزة ومُتحقَّقة حيًّا.** طبقة `useSectorContext` صارت السياق القطاعيّ العابر في العرض·الوصول·المطابقة·التوصية·البحث·الإضافة، مع أسطح كتابة/تحرير معزولة واستثناءات مبرّرة موثّقة. **251 اختبارًا أخضر** · typecheck نظيف. `interestedSectors` لم تعد بيانات ميتة — تحرّك التجربة كلها.*

## 8. مراجعة ما قبل الدمج (code-review · 8 زوايا) — أُصلحت

مراجعة نقديّة متعدّدة الزوايا كشفت 4 إصلاحات ماديّة (طُبّقت ومُتحقَّق منها حيًّا):

1. **البذر الافتراضيّ على السياق الصريح فقط:** كان `sectorScope` يبدأ «قطاعاتي» متى `has` (يشمل المشتقّ من المهارات) → كل مستخدم بمهارات افتراضيّة يرى سوقًا **مُصفّى/فارغًا** أول تحميل. أُضيف `hasExplicit`، والبذر الآن على الاختيار الصريح فقط (المشتقّ يرتّب لا يُصفّي). تحقّق حيّ: مشتقّ-فقط→«الكل» 18، صريح→«قطاعاتي» 11.
2. **استبعاد S21 من السياق:** «أخرى/يحتاج تصنيف» (كتلة حوكمة) كان قد يُبذَر في حقل `CreateOpportunity` (خارج الخيارات → فراغ) ويُقترَح في تفضيلات الملف. أُصلح **في المصدر** (`useSectorContext` يستبعد S21) فتنحلّ كل الأسطح دفعةً.
3. **`SettingsPage` تفاعليّة:** استُبدلت النسخة المحليّة المتيبّسة لـ`interestedSectors` بـ`computed` من المتجر — لا تعارض مع مزامنة Laravel.
4. **ترتيب توصية Dashboard:** أُضيف `matchRate` المنسّق كاسر تعادل أخير — يحفظ الترتيب المنسّق لملفٍّ فارغ بلا سياق.

*غير حاجب (مؤجَّل بوعي):* تكرار شريحة النطاق في 4 صفحات (مرشّح لـ`<SectorScopeToggle>`)، `rankSearch` غير مستهلَك حاليًّا، وإعادة حساب القطاع لكل مقارنة فرز (قوائم mock صغيرة).
