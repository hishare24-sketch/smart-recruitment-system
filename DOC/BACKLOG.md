# الأعمال المؤجّلة — خطة مرتّبة (أهمية + تراتبية، بلا تداخل)

> حصر المؤجّلات بعد اكتمال خطة توحيد التصنيف (0–4) وترحيل Messages/Settings لـTailwind.
> **المبدأ الحاكم لمنع التداخل (توافق ضمني):**
> 1. بنود **المنطق** (مطابقة/تصنيف) تُنفَّذ الآن لأن أهدافها البصرية (بطاقات الفرص/الطلبات · TaxonomyTree ·
>    StatCard · EmptyState) **مُرحّلة لـTailwind سلفًا** → صفر إعادة عمل.
> 2. البنود **المرتبطة بصفحة Vuetify باقية** تُطوى **داخل** ترحيل تلك الصفحة (لا خطوة منفصلة) → لا لمس مزدوج.
> 3. **الثوابت** (قوائم script-level) تُركّز الآن → تنجو من أي إعادة كتابة لاحقة للقالب.

المصادر: [`TAXONOMY_PLAN.md`](./TAXONOMY_PLAN.md) (مكتملة) · [`MIGRATION_CHECKLIST.md`](./MIGRATION_CHECKLIST.md) (Tailwind/النشر) + مسح ذرّي بـ3 وكلاء.

---

## المسار أ — إتمام محور التصنيف (منطق · آمن الآن · لا تداخل)

### ✅ B1 — توصيل محرّك المطابقة `matchScore` حيًّا — مُنجز (الفرص)
- [x] جسر نقيّ `src/services/matchProfile.ts`: `seekerMatchProfile` (القطاع يُشتقّ من المهارات عبر
      `dominantSector` إن لم يُصرّح) · `opportunityMatchProfile`/`requestMatchProfile` (القطاع من
      department/field عبر `sectorForField`). **7 اختبارات.**
- [x] `OpportunityCard` يعرض نسبة تطابق **حيّة** (من ملف الباحث الحقيقي: مهارات + موقع + نوع مفضّل).
- [x] `OpportunitiesPage`: الفرز «الأعلى تطابقاً» و«الأكثر تطابقًا» يستخدمان نفس المحرّك (تناسق تام).
- [x] `OpportunityDetailsPage`: الحلقة + بطاقات المشابهة بالتطابق الحيّ.
- **مُتحقَّق حيًّا:** طيف نسب حقيقي (72/63/57/44/…/0 حسب تطابق القطاع/المهارات/الموقع/النوع) بدل الأرقام
      المبذورة الموحّدة؛ بطاقة الفرصة #1 = شارة «الأكثر تطابقًا» = حلقة التفاصيل = **72%** (تناسق). 0 أخطاء
      console. typecheck + **231 اختبار** + build خضراء.
- **باقٍ (B1b، اختياري لاحقًا):** تطبيق نفس النمط على بطاقات سوق الطلبات (`requestMatchProfile` جاهز).

### ✅ B2 — جسر `RequestKind` القانوني + تطابق حيّ للطلبات — مُنجز
**قرار تصميمي:** الاستبدال الكامل بالأنواع الثمانية **مرفوض لأنه مُفقِد** (`project`+`consultation`
كلاهما → «عمل حر»، يُلغي تمييزًا حقيقيًّا في سوق الأعمال الحرّة). لذا: `RequestKind` **يبقى** محورًا
متمايزًا («شكل ارتباط» أدقّ للطلبات) لكنه **مُجسَّر قانونيًّا** للأنواع الثمانية، + تطابق حيّ (تكافؤ مع B1).
- [x] `RequestsStore`: `opportunityTypeOf(kind)` (جسر عام عبر `OPP_TYPE_FROM_REQUEST_KIND`) — يُخرج
      الجسر من الاختبار لواجهة المخزن العامة. **اختبار جديد.**
- [x] `requestMatchProfile` (قطاع من `field` + مهارات + موقع، بلا نوع كي لا يُعاقَب العمل الحرّ).
- [x] تطابق حيّ على `RequestsFeedPage` (البطاقات + الفرز + «الأعلى تطابقًا») و`RequestDetailsPage`
      (الحلقة + المشابهة) — بدل `matchRate` المبذور.
- **مُتحقَّق حيًّا:** طيف حيّ للطلبات 79/69/53/27/17 (يخالف المبذور 94/81/88/76/72)؛ «الأعلى تطابقًا»
      انتقل للطلب #3 (الأعلى حيًّا)؛ حلقة تفاصيل #3 = **79%** (تناسق مع العلف). 0 أخطاء console.
      typecheck + **232 اختبار** + build خضراء.
- **ملاحظة:** `kind`/`KIND_META` يبقيان للعرض (وظيفة/مشروع/استشارة/مهمة — غير مُفقِد)؛ الجسر متاح
      لأي فلترة/عرض عابر لاحق. **بذلك زال «التشتّت» (لا مفردة يتيمة) دون خسارة تعبيرية.**

### ✅ B3 — إقفال ربط محور القطاع طرفًا لطرف — مُنجز
- [x] `TaxonomyTree` يقبل `sector?` لكل عنصر ويصنّفه بالمهارة **أو** القطاع المشتقّ (`inCategory`)؛
      + أولوية العرض «أهمّ 8 + المزيد» (`displayTree`/`hiddenCount`، TAXONOMY مرتّبة بالأولوية أصلًا).
- [x] `OpportunitiesPage`/`RequestsFeedPage`: `treeItems` تمرّر `sector: sectorForField(...)?.id`،
      وفلتر التصنيف يطابق القطاع المشتقّ أيضًا (لا المهارات فقط).
- **مُتحقَّق حيًّا:** شجرة الفرص صارت **5 تصنيفات** بدل 3 (ظهرت «المالية والمحاسبة» و«المبيعات والتسويق»
      من `department` بلا مهارة معروفة؛ التقنية 8→11)؛ الفلترة بـ«المالية» تُظهر فرصة `department='المالية'`
      التي كانت خفيّة سابقًا. زرّ «المزيد» يظهر فقط عند >8 تصنيفات (غير مفعّل بالبيانات الحالية، منطقه سليم).
      0 أخطاء console. typecheck + **232 اختبار** + build خضراء. **بذلك `sectorForField` مُستهلَك حيًّا لا مجرّد مختبَر.**

---

## المسار ب — تنظيف سريع (تافه · script-level · آمن أي وقت)

### B4 — مركزة القوائم الثانوية المكرّرة
- `COMPANY_SIZES` مكرّرة في **3 ملفات** (`RoleSwitcher.vue` · `interfaces/RoleProfiles.ts` · `ProfilePage.vue`)
  → مصدر واحد `services/common.ts`.
- `EMPLOYMENT_TYPES` المحلية في `ProfilePage.vue:67` → تستهلك `OPPORTUNITY_TYPES`.
- **بلا تداخل:** تغييرات ثوابت في `<script>` تنجو من ترحيل Tailwind اللاحق.

---

## المسار ج — ترحيل Tailwind المتبقّي (كبير) — تُطوى فيه البنود المرتبطة

### B5 — بقية صفحات Vuetify (~73 ملفًا) صفحةً بصفحة
عند ترحيل كل صفحة **تُطوى بنودها المرتبطة داخل نفس الدفعة** (لا خطوات منفصلة):
- `ProfilePage` → أضِف **منتقي قطاع لتفضيلات الباحث** (`preferred_fields`) + وائم `LEVEL_LABELS`
  مع `EXPERIENCE_LEVEL_LABELS` — أثناء إعادة الكتابة لا قبلها.
- صفحات الاستبيانات → وسم خيارات `questionBanks` بالقطاعات (تحليلي، اختياري) أثناء ترحيلها.
- أبناء `SettingsPage` المضمّنون (`PublicProfileManagePage`/`AccountPlanPage`/`ThemeCustomizer`).
- المرشّحون التالون (من MIGRATION_CHECKLIST): `InterviewersMarketplacePage` · `SurveysPage` · `ProfilePage`.

### B6 — نزع حزمة Vuetify بعد آخر صفحة.

---

## المسار د — البنية التحتية (مستقلّ · الأخير)

### B7 — المرحلة 6: Docker + Nginx + staging + CI (من MIGRATION_CHECKLIST).

---

## مؤجّل بوعي / لن يُنفَّذ الآن
- **منتقي قطاع في onboarding:** لا مكوّن onboarding قائم — يتبع بناء التدفّق نفسه (خارج النطاق حتى يُبنى).
- **حذف محوّل `taxonomy.ts`:** **يبقى** — adapter رقيق نظيف يوحّد خريطة اللون Base→Vuetify في مكان واحد؛
  حذفه وتوصيل الـ9 مستهلكين مباشرةً جهد بلا قيمة تُذكر.

---

## الترتيب التنفيذي المقترح
`B1 → B2 → B3 → B4` (كلها منطق/ثوابت آمنة، تُكمل محور التصنيف) **ثم** `B5 → B6` (Tailwind الكبير، بطيّ البنود
المرتبطة) **ثم** `B7` (النشر). كل دفعة: typecheck + اختبارات + build + تحقّق حيّ + commit — كالمعتاد.
