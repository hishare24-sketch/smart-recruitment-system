# قائمة تحقّق التحويل إلى مكدّس الفريق (حيّة)

> تتبّع تقدّم التحويل مرحلةً بمرحلة. الخطة الكاملة والقرارات في [`ARCHITECTURE.md`](./ARCHITECTURE.md).
> الهدف: **Vue SPA + NestJS + JWT (`api/`) + Docker/Nginx/staging + Tailwind**، عقد `api/openapi.yaml` مصدر الحقيقة، Supabase محوّل يُنزع.

**📍 الموضع الحالي:** انتهت المراحل 1–4 (NestJS الباك-إند الوحيد). **المرحلة 5 (Vuetify→Tailwind) متقدّمة جدًّا** — مُحوّل 100%: القشرة · الرئيسية · المصادقة · الفرص (قائمة+تفاصيل+الشجرة) · المحفظة · الطلبات (feed+تفاصيل) · كل صفحات الخبراء · Onboarding · GovernanceDashboard · PageHeader/EmptyState. **~25 بديلًا في `src/components/ui/`** (أحدثها `BaseRating`). **مُحوّل أيضًا:** الرسائل (`MessagesPage`) · **مركز الإعدادات (`SettingsPage`) بكل أبنائه المضمّنين** (`AccountPlanPage`/`ThemeCustomizer`/`PublicProfileManagePage` — أُنجزت هذه الدفعة). **اكتملت أيضًا مهمتان:** تكافؤ حسابات الخبراء + دمج التصنيف المتكامل (`src/services/personas.ts`). **✅ رُحّلت `ProfilePage`** (أضخم صفحة، 226 وسمًا) **+ المكوّنان المشتركان `TrustScoreCard`/`ReviewsPanel`** (فصارت ProfilePage 0 Vuetify نهائيًّا). **✅ رُحّلت `InterviewerDashboardPage`** (148) **و`ResumeBuilderPage`** (77) **و`CandidateProfilePage`** (72) **و`SurveyAdminPage`** (72) **و`UnifiedHubPage`** (69). **✅ رُحّلت `PeerRequestsPage`** (49) **و`CompanyWishesPage`** (47) **و`AssessmentsPage`** (42) **و`AssessmentResultPage`** (29) **و`AdminDashboardPage`** (27). **✅ رُحّلت `AssistantPage`** (21) **و`SearchResultsPage`** (20) **و`ApplicationsPage`** (18) **و`UnifiedAnalyticsPage`** (18). **⏭️ التالي:** بقية صفحات Vuetify الأخفّ (المرشّح: `TakeAssessmentPage` 16 · `AnalyticsPage` 15 · `NotificationsPage` 14 · `SurveysHubPage`/`SurveysParticipatePage` · مكوّنات interviews/surveys/public). **كل الصفحات ≥18 وسمًا مُنجزة.** **⚠️ `PublicUserProfilePage` (153) مؤجّلة عمدًا** — تحمل محرّك ثيم لكل صفحة (`VThemeProvider` + قواعد `:deep(.v-card/.text-*/.bg-*)` تصبغ الصفحة بألوان صاحبها `--pp-*`)، فترحيلها يتطلّب إعادة كتابة طبقة التنسيق كاملةً وتحقّقًا عبر ثيمات الملفّات (فاتح/داكن/مخصّص) — تستحق جلسة مخصّصة. ثم بقية الوحدات (~55 ملفًا)، ثم نزع حزمة Vuetify، ثم المرحلة 6. **✅ كل المحلّي مدفوع لـ origin.**

---

## ✅ المرحلة 1 — أساس الباك-إند (NestJS) — مُنجزة ومُتحقَّقة حيًّا
- [x] مشروع `api/` (NestJS 10): prefix `/api/v1`، CORS، ValidationPipe (422 نمط Laravel)
- [x] غلاف استجابة `{ data }` + فلتر أخطاء عام + نقطة `health`
- [x] قاعدة قابلة للتبديل: sql.js (تطوير) / Postgres (إنتاج) بمتغيّر `DB_CONNECTION`
- [x] كيان `User` (uuid/role/phone، password select:false + bcryptjs)
- [x] مصادقة JWT: register · login · me · logout — مختبَرة حيًّا (curl)

## ✅ المرحلة 2 — موارد الباك-إند (لكل مورد في `openapi.yaml`) — مُنجزة ومُتحقَّقة حيًّا
لكل مورد: كيان (Entity) → وحدة (Module) → متحكّم (Controller) → حارس/صلاحية (Guard) → اختبار.
الحقول المتداخلة (مهارات/إثباتات/تفضيلات…) تُخزَّن أعمدة `simple-json` لتطابق شكل المخزن الواحد في الواجهة؛ كل مورد مملوك للمستخدم عبر `@CurrentUser()` + `JwtAuthGuard`.
- [x] `profile` (المهارات/الإثباتات/الخبرات/الشهادات + طلبات الإثبات) — `src/profile/`
- [x] `public-profiles` (الصفحة العامة بلا مصادقة + تحرير المالك `/me` بـ slug مشتقّ + مشاهدة/متابعة/تقييم/تعليق/تواصل/جدولة/توصية/طلب إثبات) — `src/public-profiles/`
- [x] `account/plan` + `wallet` (رصيد ترحيبي 100؛ الترقية تخصم الفرق من المحفظة → 402 عند نقص الرصيد؛ tier على User) — `src/account/`
- [x] `surveys` (+ الردود؛ حدّ إنشاء حسب الباقة free=1/pro=10/elite=∞ → 403؛ الردّ يصرف من مجمّع النقاط) — `src/surveys/`
- [x] `opportunities` + `requests` (بذور أوليّة + فلترة q/category/type + تقديم + طلباتي) — `src/marketplace/`
- [x] `interviews` + `interviewers` (بذور مقيّمين + حجز pending + PATCH قبول/رفض/إكمال بتقرير) — `src/interviewers/` + `src/interviews/`
- [x] `notifications` (إشعار ترحيبي عند أول وصول + تعليم الكل مقروءًا + `push()` داخلي للتدفقات) — `src/notifications/`
- **التحقّق:** `test/phase2.e2e-spec.ts` — 12 اختبار تكامل e2e يمرّ على كل مورد عبر HTTP الكامل (`npm test`)، + جولة curl حيّة مقابل `dev.sqlite` (رقّي المخطط تلقائيًا: عمود tier + الجداول الجديدة).
- المرجع الحيّ: [`../supabase/migrations/`](../supabase/migrations/) + [`CLOUD_SYNC.md`](./CLOUD_SYNC.md)

## ✅ المرحلة 3 — ربط الواجهة بالعقد (بلا لمس الشكل) — مُنجزة ومُتحقَّقة حيًّا
- [x] `.env.development`: `VITE_USE_REAL_API=true` + `VITE_BASE_API_URL=http://localhost:8000/api` (المسارات تحمل `/v1/...`؛ النسخة الحيّة غير متأثّرة — بناء إنتاج و`.env.production` فارغ)
- [x] **طبقة العميل تفكّ غلاف `{ data }`** للباك-إند (`unwrapEnvelope` في `src/services/api/index.ts`) + مساعد `put` — لتصل المخازن حمولة صافية بشكل mock
- [x] تحويل **المصادقة** عبر `api.auth.*` — للـ NestJS الأولوية عند تفعيل المفتاح (`AuthService.login/register/logout`)، ومُطابِق `fromNestUser`، و`realAuthEnabled = USE_REAL_API || supabaseEnabled` — **مُتحقَّق حيًّا:** دخول + تسجيل عبر الواجهة → توكن JWT حقيقي من NestJS في `authUser` (id من القاعدة، لا mock-token)، بلا أخطاء console
- [x] **ProfileStore** — إماهة من `api.profile.get()`+`proofRequests()` عند الدخول (الخادم مرجع الحقيقة؛ الفارغ يعود للبذرة تفاديًا لتسرّب كاش مستخدم سابق)، و`persist` يدفع اللقطة الكاملة PATCH مُمهَّلًا (600ms) خلف علَم `ready`، و`resolveProofRequest` عبر النقطة. **الواجهة بلا تغيير.** توسعة الخادم: عمود `prefs` + `UpdateProfileDto` يقبل الوثيقة الكاملة (skills/experiences/certificates/prefs). **مُتحقَّق حيًّا:** بعد مسح الكاش وإعادة التحميل عادت البيانات من NestJS (عنوان + 5 مهارات)، بلا أخطاء console
- [x] **PublicProfileStore** — إماهة المالك من `GET /public-profiles/me` (كتلة `doc` + عدّادات `stats` الحيّة تعلوها؛ الفارغ يبقي البذرة)، حفظ مُمهَّل `PATCH /me {doc}`، وأفعال الزوّار (view/follow/rate/comment/testimonial/contact/schedule/requestProof) تُرسَل لنقاط الخادم. توسعة الخادم: عمود `doc` + `GET /me` + `present()` يُسطّح `doc` ويُخفي `inbox`/`doc`. **مزامنة Supabase مُعطَّلة عند `USE_REAL_API`** (نُزعت فعليًّا في هذه الدفعة — كانت تصارع NestJS على الصفحة العامة). **مُتحقَّق حيًّا:** تحرير العنوان + متابعة + تقييم زائر → حُفظت في NestJS، وبعد مسح الكاش وإعادة التحميل عادت من الخادم؛ 0 نداء Supabase، بلا أخطاء console
- [x] **آلية عامة للمستندات الخاصة (blob):** مورد خادمي `GET/PUT /account-states/:store` (كيان `AccountState` بمفتاح فريد userId×store) يقابل جدول Supabase `account_states`. `syncPrivateDoc` يُوجَّه إليه عند `USE_REAL_API` (إماهة + حفظ مُمهَّل خلف `ready`) لكل مخزن **مُدرَج** في `NEST_PRIVATE_STORES` (يتوسّع مخزنًا بمخزن؛ المخازن ذات المورد المخصّص تُوصَل عبر موردها لا هنا). الحقن الصريح للاختبار يتجاوز التوجيه.
- [x] **Marketplace** — `RequestsStore` (طلباتي) · `PostedOpportunitiesStore` · `ApplicationsStore` (كلها blob خاص) صارت تُحفظ في NestJS عبر الآلية العامة (بلا أي تغيير في المخازن — فقط إدراجها). كتالوج الفرص/الطلبات الغنيّ يبقى بذرة عميل (بيانات عرض). **مُتحقَّق حيًّا:** تقديم على طلب → PUT مُمهَّل لـ account-states → مسح الكاش + reload → عادت التطبيقات الخمسة من الخادم، `syncStatus:synced`، بلا أخطاء console
- [x] **Interviews + Interviewers** — `InterviewsStore` (مقابلاتي بنتائجها الغنية) · `InterviewersStore` (ورشة المقيّم: حجوزات/أجندة/تسعير/عناصر تقييم) أُدرجا في `NEST_PRIVATE_STORES` فصارا يُحفظان في NestJS عبر الآلية العامة (بلا تعديل عليهما). **ملاحظة:** المخزنان أغنى من موردَي `/interviews` و`/interviewers` المبسّطَين فاعتُمد الـ blob (أوفى لبياناتهما الغنية). كتالوج المقيّمين يبقى بذرة عرض. **مُتحقَّق حيًّا:** بدء مقابلة + حجز → PUT مُمهَّل → مسح كل مفاتيح المقابلات + reload → عادا من الخادم (`synced`)، بلا أخطاء console
- [x] **Surveys + Notifications** — `SurveysStore` (استبيانات + ردود + دورة حياة + استهداف) · `NotificationsStore` (إشعارات بإجراءات) أُدرجا في `NEST_PRIVATE_STORES` كـ blob (أغنى من موردَي `/surveys` و`/notifications`؛ حدّ الباقة يبقى على العميل عبر `canCreate`). **مُتحقَّق حيًّا:** إنشاء استبيان + دفع إشعار → PUT مُمهَّل → مسح الكاش + reload → عادا من الخادم (الاستبيان بردوده الـ17 + الإشعار)، بلا أخطاء console
- [x] **Wallet + Plan** — `WalletStore` (سِجِلّ مالي: عمليات/وسائل دفع) · `AccountPlanStore` (الباقة) أُدرجا كـ blob. **قرار:** المخزنان أغنى من موردَي `/wallet`+`/account/plan`، والدفع يبقى على العميل (`wallet.pay`)؛ **الدفع الخادمي الذرّي (402 حقيقي) مؤجّل لطور المدفوعات الحقيقية** (يتطلّب استبدال سِجِلّ العميل بسِجِلّ خادمي، لا مجرّد حفظ). **مُتحقَّق حيًّا:** شحن 1234 + ترقية free→pro (خُصمت رسومها من المحفظة) → مسح الكاش + reload → عاد السِجِلّ (بالإيداع ورسوم الباقة) والباقة `pro` من الخادم، بلا أخطاء console
- [x] **السحب الأخير** — أُضيفت بقية مخازن blob الـ14 لـ `NEST_PRIVATE_STORES` (messages/wishes/saved/reviews/gamification/candidates/roleProfiles/resumes/searchPrefs/expertRoles/peerRequests/roleRequests/endorser/interviewerBrand). **مُتحقَّق حيًّا:** 9 مخازن منها بلغت `synced` على الإقلاع، و`gamification` جولة كاملة (تعديل النقاط → PUT → مسح الكاش + reload → عادت 360 من الخادم)، بلا أخطاء console. **بذلك كل مخازن blob الخاصة تُحفظ في NestJS.**
- [x] **تحقّق حيّ بعد كل مخزن** — كل الدفعات أعلاه مُتحقَّقة حيًّا (register→JWT · Profile · PublicProfile · Marketplace · Interviews/Interviewers · Surveys/Notifications · Wallet/Plan · السحب الأخير). **المصادر المرجعية لكل نداءات المخازن الخاصة صارت NestJS، وSupabase مُعطَّل في الوضع الحقيقي.**
- [x] تحقّق حيّ بعد كل مخزن (Auth ✓ · Profile ✓ · PublicProfile ✓ · Marketplace ✓ · Interviews/Interviewers ✓ · Surveys/Notifications ✓ · Wallet/Plan ✓)

## ✅ المرحلة 4 — نزع Supabase + البثّ اللحظي — مُنجزة ومُتحقَّقة حيًّا
- [x] **البثّ اللحظي عبر NestJS WebSocket Gateway (Socket.IO) — بديل Supabase Realtime.** كيان `DirectMessage` + مورد `/direct-messages` (send/list/read/resolve) + `MessagesGateway` (توثيق JWT في المصافحة، غرفة لكل uuid، دفع للمستقبِل). `directMessages.ts` أُعيدت كتابته للـ NestJS REST + `socket.io-client`، و`MessagesStore.wireInbound` صار يعتمد `USE_REAL_API` + uuid الجلسة (بلا Supabase). openapi + مخطط `DirectMessage`. **مُتحقَّق حيًّا:** المستخدم A في المتصفح مشترك عبر WS، والمستخدم B يرسل من الخادم → وصلت **لحظيًا بلا تحديث** لمخزن A (محادثة peer جديدة + unread + إشعار)، عربية سليمة (✅)، بلا أخطاء console. e2e: 17 اختبارًا (منها تسليم WS بين عميلين).
- [x] استبدال `directMessages` بنداءات العقد (مُنجز أعلاه). `cloudSync` صار مُوجَّهًا لـ NestJS/معطّلًا في الوضع الحقيقي منذ المرحلة 3.
- [x] **أُزيل Supabase نهائيًّا:** نُزع فرع Supabase من `AuthService` (صار NestJS + محاكاة فقط)، وأُفرِغ محرّك Supabase من `cloudSync.ts` (بقي توجيه NestJS + fallback محلي off)، و`syncPublicProfileDoc` صار محايدًا (الصفحة العامة عبر موردها). حُذفت `src/services/supabase.ts` + `supabase.test.ts` + مجلّد `supabase/` (6 ترحيلات + schema) + `scripts/db-migrate.mjs`؛ ونُزعت حزمة `@supabase/supabase-js` + سكربت `db:migrate` + متغيّرات Supabase من `.env.development` و`deploy.yml`. أُعيدت كتابة `cloudSync.test.ts` لسلوك NestJS. **الأثر:** حجم الحزمة الرئيسية انخفض ~657KB→477KB. **تحقّق حيّ:** التطبيق يُقلع نظيفًا (21 مخزنًا، الجلسة محفوظة)، بلا أخطاء console. typecheck + **185 اختبار** + build خضراء.
- [x] تحقّق حيّ: رسالة لحظية بين مستخدمين ✓ (المرحلة 4a)

## 🟡 المرحلة 5 — الواجهة: Vuetify → Tailwind — جارية
- [x] **إعداد Tailwind (v3) بالتعايش مع Vuetify:** `postcss.config.js` + `tailwind.config.js` (**preflight مُطفأ** كي لا يُصفّر تنسيقات Vuetify) + `src/styles/tailwind.css` (مستورد قبل main.scss). **رموز التصميم مجسورة بمتغيّرات ثيم Vuetify** `--v-theme-*` (brand/accent/emerald/bg/surface/content/…) فتتزامن الألوان تلقائيًا مع تبديل الوضع؛ + `rounded-ui`/`rounded-ui-lg` من متغيّرات radius؛ + مساعدات alpha (`.border-ui`/`.text-muted`) لأن رموز Tailwind لا تدعم alpha على متغيّرات Vuetify المفصولة بفواصل.
- [x] **مكوّنات أساس أولى** (`src/components/ui/`): `BaseButton` (5 أنماط×3 أحجام + loading/disabled) · `BaseCard` · `BaseChip` (نغمات دلالية). معرض `/ui-kit` للتحقّق. **مُتحقَّق حيًّا:** BaseButton brand = `rgb(163,230,53)` مطابق تمامًا لـ `--v-theme-primary`، radius 12px، الرقائق تُصيّر؛ **وVuetify سليم تمامًا** (زر الدخول #BEF264، الحقول radius 12px، العناوين 700) — أي التعايش بلا كسر. بلا أخطاء console.
- [x] **بدائيات القشرة** (`src/components/ui/`): `BaseIcon` (خطّ @mdi/font مباشرةً بلا VIcon) · `BaseAvatar` (دلالي/tonal/square) · `BaseBadge` (نقطة/عدّاد overlay بمواضع منطقية RTL + inline) · `BaseDropdown` (مُشغّل + لوحة، **إغلاق عبر طبقة backdrop كاملة الشاشة** لا مُصغي مستندي — بعد أن ثبت أن سباق pointerdown/الـTransition هشّ؛ + Escape + محاذاة RTL + closeOnContent). أُضيفت كلها لمعرض `/ui-kit`.
- [x] **القشرة والتخطيط — `DefaultLayout.vue` أُعيد كتابته بالكامل Tailwind** (نُزع VNavigationDrawer/VAppBar/VMain/VBottomNavigation/VList/VMenu/VTabs). قائمة جانبية `fixed` (270px، مطوية rail 76px على سطح المكتب، overlay + backdrop على الموبايل عبر `rtl:/ltr:translate-x-full`)، شريط علوي `fixed` مُزاح بعرض القائمة، تنقّل سفلي للموبايل، محتوى `<main>` بحشوات منطقية. **الوضع المختلط:** القشرة ملفوفة بـ `<div class="v-theme--{chromeThemeName}">` فتُضبط متغيّرات `--v-theme-*` الداكنة لفرعها (بديل موثوق لـ VThemeProvider الذي لم يُصيّر عنصرًا). فئات حالة alpha (`.nav-link`/`.nav-tab`/`.menu-row`/`.icon-btn`/`.nav-tile`/`.dd-panel`) في `tailwind.css`. + مفتاح i18n `common.menu`. **مُتحقَّق حيًّا:** القائمة/الشريط يُصيّران، التبويبان (منصة/حسابي) يبدّلان ويلوّنان (نشط=brand 163,230,53 + خلفية 0.14 + 700)، تمييز الرابط النشط (`router-link-exact-active`=brand)، القائمتان المنسدلتان (الثيم + المستخدم) تفتحان وتُغلقان بالنقر خارجهما وEscape، الموبايل يُخفي القائمة خارج الشاشة + تنقّل سفلي 5 عناصر، سطح المكتب permanent + إزاحة 270px. **الأوضاع الثلاثة:** داكن (قشرة+محتوى داكن) · فاتح (كلاهما فاتح) · **مختلط (قشرة داكنة 22,34,27 + محتوى فاتح 255,255,255 ✓)**. typecheck + 185 اختبار + build خضراء، بلا أخطاء console.
- [ ] بقية الأساس التفاعلي (Input/Dialog/…) عند الحاجة أثناء ترحيل الصفحات
- [ ] **الصفحات صفحةً بصفحة** (جارٍ):
  - [x] **رئيسية الباحث/الشركة** (`DashboardPage.vue`) — أُعيدت كتابتها Tailwind بالكامل (نُزع VCard/VRow/VCol/VList/VAlert/VProgressCircular/VTimeline/VChip/VBtn/VAvatar/VIcon). بديل جديد `BaseProgressRing` (SVG، لون دلالي)، وتوسعة `BaseButton` (polymorphic `to`→RouterLink + نغمات `tonal-brand/accent/emerald` + `align`). **مُتحقَّق حيًّا (نسختا باحث+شركة):** حلقة الثقة (قوس success بنسبة 77% + مركز «77»)، شبكة الإحصاء 4 أعمدة، العمود الرئيسي span-2، تنبيهات AI بحدّ ملوّن حسب النغمة، أزرار المساعد tonal-brand، أعمدة تحليلات الشركة (4) + قائمة المرشّحين (5). **مكسب جانبي:** زالت تحذيرات «ref owner context» لأن VProgressCircular/VTimeline رُفعا.
  - [x] **مكوّنان مشتركان** (شجرة الرئيسية): `StatCard` + `OpportunityCard` صارا Tailwind. بديل جديد `BaseProgressBar` (شريط خطّي، لون دلالي) + توسعة `BaseAvatar` (success/info/error). **مُتحقَّق حيًّا:** الرئيسية (بطاقات إحصاء بأفاتار tonal + بطاقات فرص بشريط تطابق 94% أخضر success) والصفحة المستهلِكة `/opportunities` (18 بطاقة بشريط + 36 زر إجراء) — بلا كسر في التعايش، 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`GamificationCard` (آخر طفل في الرئيسية)** صار Tailwind (3 أشرطة تقدّم عبر BaseProgressBar، رقاقة الرتبة بلون `g.tier.color` المخصّص، تلميحات الشارات عبر `title` الأصلي بدل VTooltip). **✅✅ الرئيسية صارت Tailwind 100% — `.v-card` في `<main>` = 0.** **مُتحقَّق حيًّا:** رتبة «فضي» بخلفية `rgb(156,163,175)` ونص أبيض، 2 شارة مكتسبة + 5 مقفلة، 9 أشرطة تقدّم، 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`OpportunityDetailsPage` صارت Tailwind 100%** (`.v-card/.v-btn/.v-dialog` في `<main>` = 0). بدائيّان جديدان: `BaseModal` (نافذة حوار مُنقولة لـ `<body>` عبر Teleport، إغلاق بالخلفية/Escape/زر، عنوان + أزرار slots، دخول CSS بلا `<Transition>`) و`BaseSnackbar` (إشعار سفلي مُنقول، اختفاء تلقائي بعد timeout). حلقة التطابق `BaseProgressRing` (110px)، أشرطة التحليل `BaseProgressBar`، اختيار السيرة radio-cards. **مُتحقَّق حيًّا:** العنوان + حلقة 94% + 4 أشرطة + 5 أقسام + مشابهة؛ **دورة التقديم كاملة:** فتح النافذة (title + سيرتان radio + تأكيد) → اختيار السيرة الثانية → تأكيد → النافذة تُغلق + إشعار «تم إرسال طلبك» + الزر يصير «تم التقديم» + الإشعار يختفي تلقائيًّا. Escape/الخلفية يغلقان. 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`OpportunitiesPage` + `PageHeader` المشترك** صارا Tailwind. بدائيّان جديدان: `BaseSelect` (**عام generic**، `<select>` أصلي منسّق + خيارات بلون صريح للوضع الداكن + `clearable`) و`BaseSlider` (`input[type=range]` + `accent-color` من الثيم). الصفحة تستهلك `OpportunityCard` المحوّل؛ **مُتحقَّق حيًّا:** 4 قوائم + منزلق + بحث + 22 رقاقة/زر + 18 بطاقة فرصة + شجرة؛ فلترة المدينة 18→8، المنزلق (راتب 10000)→1، تبديل رقاقة ذكية، تبديل عرض شبكة↔قائمة، إعادة تعيين→18. `PageHeader` (مشترك، props ثابتة) تحقّق أيضًا على `/achievements`. 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`RequestDetailsPage` (تفاصيل الطلب) Tailwind 100%** → **مسار الطلبات كامل**. بديل جديد `BaseTextarea`؛ الأسئلة الشائعة عبر `<details>/<summary>` أصلي (أكورديون بلا مكوّن)؛ هيكل تحميل التفاوض عبر `animate-pulse`. **مُتحقَّق حيًّا:** العنوان + حلقة 94% + 3 أكورديون (فتح/إغلاق) + 4 أشرطة تحليل؛ نافذة التفاوض (هيكل تحميل → مسودة AI 304 حرفًا في textarea → Escape يغلق). 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`RequestsFeedPage` (سوق الطلبات) Tailwind 100%** (`vuetifyInMain`=0). بدائيّان جديدان: `BaseDrawer` (درج جانبي Teleport من الجهة المنطقية end، إغلاق خلفية/Escape، الخلفية v-if فوري بلا Transition) و`BaseSwitch` (مفتاح تبديل، thumb عبر justify-start/end المنطقي RTL). يستهلك `TaxonomyTree` المحوّل. اقتراحات البحث الذكية لوحة CSS-animated. **مُتحقَّق حيًّا:** بحث + 5 بطاقات بحلقات تطابق + 4 رقائق ذكية + top-match؛ درج الفلترة (شجرة + 4 أنواع + مجال + منزلقان + مفتاح): المفتاح «عن بُعد» يفلتر 5→4، نوع «وظيفة» يركّب لـ0، إعادة تعيين→5، إغلاق بالخلفية والدرج ينزلق خارج الشاشة؛ اقتراحات البحث (تركيز→4 اقتراحات، نقر يطبّق). 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`WalletPage` (المحفظة) Tailwind 100%** (`vuetifyInMain`=0) — أضخم صفحة حتى الآن (4 نوافذ + 3 رسوم + سجل + وسائل دفع). حُوّل معها المشتركان `EmptyState`. `BaseInput` وُسِّع ليحترم `v-model.number` (يقرأ `modelModifiers`) + قبول `null`. النوافذ الأربع عبر `BaseModal`، الإشعار عبر `BaseSnackbar`، تبويبا «بطاقة/بنك» segmented inline، تلميحات الرسوم عبر `title`، الرسم الدائري conic-gradient يبقى كما هو. **مُتحقَّق حيًّا:** إضافة وسيلة دفع (تبويبان + تحقّق `methodValid` + حفظ + إشعار + ظهور في القائمة)، **الشحن دورة كاملة** (مبلغ سريع 500 → اختيار وسيلة → تأكيد → الرصيد 556,185→556,685) — وزر التأكيد يُعطّل صحيحًا حين لا وسيلة، فلترة السجل (4→3→4)، `EmptyState` يظهر لنوع فارغ («سحب»). 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`TaxonomyTree` (المكوّن المشترك)** صار Tailwind → **`OpportunitiesPage` Tailwind 100% (`vuetifyInMain`=0)**. صفوف التصنيف/الفرع أزرار بحالة نشطة، بحث بـ BaseInput، عدّادات BaseChip، ألوان التصنيفات من رموز الثيم. **مُتحقَّق حيًّا:** اختيار تصنيف 18→8، توسيع يُظهر الفروع، اختيار فرع 8→1، إزالة الكل→18؛ ويعمل أيضًا في مستهلكه `/interviewers` (تعايش). 0 أخطاء. typecheck + 185 اختبار + build خضراء.
  - [x] **`WishesPage` (الرغبات الواردة) Tailwind 100%** (`vuetifyInMain`=0). نُزع VRow/VCol/VCard/
        VAvatar/VChip/VBtn/VDialog/VTextField/VTextarea. يستهلك StatCard المشترك، نافذة تفاوض `BaseModal`
        (#actions). **مُتحقَّق حيًّا:** 4 بطاقات رغبات + نافذة التفاوض (3 حقول + زرّان). 0 أخطاء console.
  - [x] **`InterviewsPage` (المقابلات) Tailwind 100%** (`vuetifyInMain`=0). نُزع VCard/VIcon/VAlert/
        VChip/VBtn/VList/VListItem/VAvatar/VRow/VCol/VDivider/VDialog. شريط التقويم الأسبوعي بقي بـCSS
        scoped، قائمة القادم `RouterLink`، نافذة الإعداد `BaseModal` ببطاقات مسار/مستوى `.is-selected`.
        **مُتحقَّق حيًّا:** شريط أسبوعي + 4 أنواع + نافذة إعداد باختيار مستوى يعمل. 0 أخطاء console.
        **درس:** انتبه لإغلاق `computed(() => Array.from(...))` بـ`}))`.
  - [x] **`SurveysPage` (منشئ الاستبيانات) Tailwind 100%** (`vuetifyInMain`=0). نُزع VRow/VCol/VCard/
        VAvatar/VIcon/VChip/VBtn/VTable/VProgressLinear/VTooltip/VDialog/VTabs/VWindow/VTextField/
        VSelect/VAlert/VSwitch/VCombobox/VTextarea/VExpandTransition/VSnackbar. **بديل جديد
        `BaseTagInput`** (إدخال وسوم حرّ: كتابة + Enter/فاصلة يضيف، رقائق قابلة للإزالة، Backspace يحذف —
        بديل VCombobox multiple لتحرير خيارات/عناصر الأسئلة). الجدول HTML منسّق + تلميحات عبر `title`،
        نافذة المنشئ ثلاثية الخطوات `BaseModal` (تبويبات `.seg` + `v-if` للخطوة + slot `#actions`)، أزرار
        الأنماط العشرة `btn-tonal-brand`، رابط خارجي `<a>` منسّق. **دروس:** slot تذييل BaseModal اسمه
        `#actions` (لا `#footer`) وعرضه `:max-width` (لا `size`)؛ BaseSnackbar `color` اسم ثيم Vuetify
        (primary لا brand). **مُتحقَّق حيًّا:** 0 Vuetify، 8 بطاقات نماذج + جدول، فتح المنشئ (عنوان +
        3 تبويبات + أزرار `#actions` حفظ/نشر)، **BaseTagInput كامل** (كتابة «خيار ثالث» + Enter → أُضيف
        لخيارات السؤال). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`InterviewersMarketplacePage` (سوق المقيّمين) Tailwind 100%** (`vuetifyInMain`=0). نُزع
        VBtn/VDialog/VCard/VChip/VIcon/VRow/VCol/VAvatar/VAutocomplete/VSlider/VTextField/VSelect/
        VBtnToggle/VList/VProgressLinear/VAlert/VSnackbar. **بديل جديد `BaseMultiSelect`** (اختيار متعدّد
        قابل للبحث + رقائق قابلة للإزالة + لوحة backdrop — بديل VAutocomplete multiple، سيُعاد استخدامه
        في ProfilePage). رقائق الفلتر أزرار بحالة نشطة (`chipStyle` inline بلون الثيم)، عرض segmented
        `.seg`، النافذتان (المستويات + إعادة الجدولة) `BaseModal`، `mapColor` موسّع (`blue-grey`→neutral).
        شجرة التصنيف تمرّر `sector` (تكامل B3). **مُتحقَّق حيًّا:** 0 Vuetify، الموصى بهم + 5 مقيّمين +
        الحجوزات، **BaseMultiSelect كامل** (تركيز→فلترة «Vue»→«Vue.js»→نقر→رقاقة + فلترة 5→1)، نافذة
        المستويات (4 مستويات). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **الأبناء المضمّنون للإعدادات — Tailwind 100%** (أُنجزت الدفعة الموعودة): **`AccountPlanPage`** (بطاقات الباقات الثلاث + شبكة الاستهلاك عبر BaseProgressBar؛ **درس:** `ACCOUNT_TIER_META.free.color='surface-variant'` ليس لونًا صالحًا لمكوّنات الأساس → وسّعت `mapColor` ليحوّل `surface-variant`/`grey`→`neutral` و`amber`→`warning`) · **`ThemeCustomizer`** (وضع العرض segmented `.seg` + 5 هويات لونية `.preset-row` + منتقيا لون أصليّان في `#suffix` لـ BaseInput) · **`PublicProfileManagePage`** (أكبرها 134 وسمًا: 4 تبويبات فرعية `.nav-tab` بـ`v-else-if` + شارة تعليقات، حلقة القوة BaseProgressRing، رقائق-كأزرار عبر helper `toggleStyle(active,color)` صلب/حدّ، BaseTagInput للكلمات المفتاحية، BaseSelect للخط، BaseModal لعمل المعرض، السحب/الإفلات وحقول اللون الأصلية بقيت CSS scoped). **مُتحقَّق حيًّا (عبر تبويبات الإعدادات):** الباقة (3 بطاقات + استهلاك، Vuetify=0) · المظهر واللغة (تخصيص المظهر + 5 هويات + منتقيا لون + تبديل الوضع فاتح↔داكن يعمل) · صفحتي التعريفية (حلقة قوة + 4 تبويبات فرعية + 16 حقلًا + المظهر: الحالة/الثيمات/الترتيب، Vuetify=0). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء. **ملاحظة أداة:** `preview_screenshot` يتعطّل في هذه البيئة (الصفحة حيّة عبر eval)، فاعتُمد التحقّق على تأكيدات DOM.
  - [x] **`SettingsPage` (مركز الإعدادات) Tailwind** (markup الصفحة نفسها بلا Vuetify؛ **صار كل أبنائه المضمّنين Tailwind أيضًا** — انظر البند أعلاه). نُزع VAutocomplete/VTabs/VWindow/VBtnToggle/VCard/VTextField/VSwitch/VSelect/VChip/VRow/VCol/VDivider/VIcon. بدائل: تبويب جانبي `.settings-tab` (helper جديد؛ عمودي md+ / أفقي قابل للتمرير على الموبايل)، **بحث الإعدادات combobox** (BaseInput + لوحة `.dd-panel` مفلترة + backdrop إغلاق)، segmented `.seg`/`.seg-btn` (helper جديد بديل VBtnToggle؛ اللغة/حجم الخط/7 صفوف خصوصية)، إشعارات BaseSwitch×6 + وسائل BaseCheckbox×3 (مصفوفة)، تكامل بطاقات شبكة. تبديل التبويب عبر `v-if` (يتجنّب تحميل الصفحات المضمّنة الثقيلة) + شارات حيّة (قوة الملف% + اسم الباقة). **مُتحقَّق حيًّا:** 7 تبويبات + شاراتها (صفحتي 80% · باقتي النخبة)، تبويب الحساب (5 حقول + حفظ، Vuetify=0)، تبديل مفتاح «تذكيرات» off→on، segmented الخصوصية «عام»→«خاص» (النشط brand صلب `rgb(163,230,53)` + نص on-primary)، **البحث كامل:** تركيز→لوحة، «الباقة»→نتيجة واحدة، نقر→قفز لتبويب «باقتي» + URL `?tab=plan` + إغلاق + تفريغ. 0 أخطاء console. typecheck + 185 اختبار + build خضراء. **درس مؤكّد ثانيةً:** helper CSS جديد لا يُطبَّق عبر HMR على عناصر موجودة → أعد التحميل الكامل قبل قياس `getComputedStyle`.
  - [x] **`MessagesPage` (الرسائل) Tailwind 100%** (`vuetifyInMain`=0) — نُزع VCard/VList/VListItem/VAvatar/VBadge/VDivider/VTextField. القائمة الجانبية أزرار `.conv-row` (helper alpha جديد في `tailwind.css`: خلفية نشطة `primary/0.14` + حدّ جانبي منطقي 2px)، فقاعات الرسائل `bg-brand text-on-brand` (أنا) / `border-ui bg-surface` (الآخر)، حقل الإرسال `BaseInput` + زر `icon-btn` في خانة suffix، شارة غير المقروء `bg-accent`. **درس:** رموز alpha لـ Tailwind (`bg-brand/15`) لا تُصيّر على متغيّرات Vuetify المفصولة بفواصل → تُعطي شفافًا؛ الحلّ helper class بـ `rgba(var(--v-theme-*), a)`. **مُتحقَّق حيًّا:** القائمة (المحادثة النشطة خلفية `rgba(163,230,53,0.14)` + حدّ brand، شارة غير مقروء)، الخيط (فقاعة «أنا» brand صلب `rgb(163,230,53)`)، **دورة إرسال كاملة:** كتابة → إرسال → فقاعة جديدة بوقت «الآن» + الحقل يُفرَغ. 0 أخطاء console. typecheck + 185 اختبار + build خضراء.
  - [x] **`ProfilePage` (الملف الشخصي) Tailwind 100%** (markup الصفحة `vuetifyInMain`=0؛ يبقى المكوّنان المشتركان المضمّنان `TrustScoreCard` + `ReviewsPanel` Vuetify مؤقتًا حتى ترحيلهما) — **أضخم صفحة رُحّلت (226 وسمًا)**. **بديل جديد `BaseRating`** (نجوم قابلة للنقر 1..max، بديل VRating). نُزع . البنية: هيرو ببانر `.brand-gradient` (BaseCard `:padded=false`) + شبكة إحصاء + شريط اكتمال، تبويبات الأدوار `.nav-tab`، 8 تبويبات باحث بـ`v-else-if`، ملفا المقيّم/الجهة (شبكات + `.seg` رؤية + BaseTagInput للحقول الحرّة + رقائق-تبديل لأنواع العمل تخزّن **ids**)، 6 نوافذ `BaseModal` + قائمة سيرة `BaseDropdown` + محور إثباتات مسطّح (نقاط ملوّنة بدل VTimeline) + إشعارا `BaseSnackbar`. **دروس:** حقول الاتحاد (`visibility`/`availability`/`company_size`) تتطلّب `as typeof الحقل` عند الإسناد من option عام؛ اقتراحات المهارة عبر `<datalist>` أصلي لا combobox؛ ألوان Vuetify الاسمية تُترجم بـ`mapColor`(للمكوّنات)/`colorVar`(للأنماط، يعالج hex وaliases مثل amber→warning). **مُتحقَّق حيًّا (مسار الباحث):** 9 تبويبات، المهارات (بطاقات + أشرطة ثقة)، التفضيلات (رقائق أنواع العمل + 2 BaseTagInput + select الجاهزية + مفتاح العرض الذاتي)، **نافذة إضافة مهارة + BaseRating** (5 نجوم، افتراضي 3، نقر الخامسة→5 مملوءة، datalist + select التصنيف، Escape يغلق). markup=0 Vuetify (الـ28 المتبقية كلها من TrustScoreCard/ReviewsPanel المضمّنين). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء (الحزمة 50.4→49.0KB). **ملاحظة:** كتلتا المقيّم/الجهة مشروطتان بملكية الدور (الحساب التجريبي باحث فقط) فتُحقَّقتا نوعيًّا لا بالنقر — بنيتهما بنفس البدائل المُتحقَّقة.
  - [x] **المكوّنان المشتركان `TrustScoreCard` + `ReviewsPanel` Tailwind 100%** → **`ProfilePage` صارت 0 Vuetify نهائيًّا (بما فيها المضمّنان)**، ويستفيد كل مستهلكيهما. `TrustScoreCard`: حلقة BaseProgressRing + BaseModal (رادار TrustRadar يبقى SVG + 8 أشرطة عوامل + بطاقات نصائح tonal) + BaseSnackbar للتحفيز. `ReviewsPanel`: **وُسِّع `BaseRating` بأنصاف النجوم** (`mdi-star-half-full` في وضع readonly لعرض المتوسّطات) — عرض المتوسط + نجوم كل تقييم + إدخال تقييم جديد، BaseAvatar/BaseChip/BaseTextarea/BaseModal، محرّر ردّ. **مُتحقَّق حيًّا على `/profile`:** `vuetifyInMain=0`، حلقة الثقة (77 + شارة المستوى)، نافذة التحليل (رادار + 8 أشرطة + نصائح، Vuetify=0)، تبويب التقييمات (17 نجمة منها **نصف نجمة في المتوسّط** + بطاقتا تقييم)، **تدفّق الرد** (فتح المحرّر → «اقتراح رد» يملأ المنطقة بردّ AI). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`InterviewerDashboardPage` (لوحة المقيّم) Tailwind 100%** (`vuetifyInMain`=0) — 148 وسمًا. نُزع VBtn/VRow/VCol/VAlert/VIcon/VCard/VList/VListItem/VListItemTitle/VListItemSubtitle/VAvatar/VDivider/VRating/VChip/VMenu/VTextField/VSwitch/VSelect/VBtnToggle/VSlider/VDialog/VCardTitle/VCardText/VCardActions/VTextarea. يستهلك المشتركات المُرحّلة (`PageHeader`/`StatCard`/`ReviewsPanel`). قوائم الأجندة (طلبات/قادمة/منفّذة) صفوف `border-t` داخل BaseCard، تلميح العمولة `BaseDropdown` (`:close-on-content=false`)، منزلق الخصم `BaseSlider` + قيمة حيّة، segmented نوع العرض `.seg`، لاحقة ﷼ عبر `#suffix`، تنبيهات النمو الثلاثة divs بحدّ `border-s-4`. 5 نوافذ `BaseModal`. **مُتحقَّق حيًّا** (مُنح دور المقيّم مؤقتًا عبر localStorage ثم أُعيد — الحساب باحث فقط والمسار محروس): `vuetifyInMain=0`، الإحصاء + الأقسام الثلاثة + 14 بطاقة، **نافذة العرض** (segmented + BaseSlider@20؛ «جلسة مجانية» تخفي المنزلق)، **نافذة الأسعار** (5 حقول رقمية بلاحقة ﷼)، **اقتراحات AI** (3 عناصر تقييم برقائق سعر). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`ResumeBuilderPage` (منشئ السيرة) Tailwind 100%** (`vuetifyInMain`=0) — 77 وسمًا. معالج 7 خطوات + **معاينة حيّة** (مستند `.resume-preview`/`.rp-*` أبيض قائم بذاته بـCSS خاص — **بقي كما هو بلا مساس**). نُزع VRow/VCol/VCard/VProgressLinear/VIcon/VSwitch/VSelect/VMenu/VList/VListItem/VBtn/VChipGroup/VChip/VTextarea/VDivider/VAvatar/VTextField/VDialog/VImg/VSnackbar/VAlert. بدائل: بطاقات القوالب أزرار قابلة للاختيار (نشط brand صلب)، segmented أنماط الملخص رقائق-تبديل، قائمة الترجمة `BaseDropdown`، بطاقات التصدير الأربع أزرار، QR `<img>` مع `@error` fallback، نافذتا QR/الرابط الخاص `BaseModal`. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، المعالج (خطوة 1/7 + 5 قوالب)، **المعاينة الحيّة تتفاعل فورًا** (قالب «تنفيذي» → رأس `rp-header--dark`؛ لغة «الإنجليزية» → المعاينة `dir=ltr` + عناوين إنجليزية «Professional Summary…»)، الخطوة 7 (4 بطاقات تصدير + اسم + 3 مشاركة)، **نافذة الرابط الخاص** (كلمة مرور مولّدة `MZ2PVN`)، **نافذة QR** (صورة من api.qrserver). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`CandidateProfilePage` (تفاصيل المرشّح) Tailwind 100%** (`vuetifyInMain`=0) — 72 وسمًا. نُزع VBtn/VRow/VCol/VCard/VAvatar/VChip/VDivider/VIcon/VProgressCircular/VProgressLinear/VDialog/VCardTitle/VCardText/VCardActions/VSelect/VAlert/VTextField/VTextarea/VSnackbar. الهيدر + المهارات + التوصيات + بطاقات التقارير (أزرار)، حلقة التطابق `BaseProgressRing`، شريط جانبي بأزرار الإجراء + `BaseProgressBar` للتحليل، 3 نوافذ `BaseModal` (طلب مقابلة بـ2 `BaseSelect` + سعر محسوب، تقرير، إبداء رغبة). **مُتحقَّق حيًّا** (مُنح دور «company» مؤقتًا عبر localStorage ثم أُعيد — المسار محروس): `vuetifyInMain=0`، حلقة التطابق + المهارات + 4 أزرار إجراء، **نافذة طلب المقابلة** (سعر يتفاعل: نوع «شامل» → 230→300 ﷼)، **نافذة التقرير** (نقاط قوة/تحسين + توصية). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`SurveyAdminPage` (إدارة الاستبيان) Tailwind 100%** (`vuetifyInMain`=0) — 72 وسمًا. نُزع VBtn/VCard/VIcon/VChip/VAlert/VRow/VCol/VProgressLinear/VTextField/VSelect/VBtnToggle/VTable/VDialog/VCardTitle/VCardText/VCardActions/VFileInput/VTextarea/VSnackbar. دورة الحياة 6 مراحل (المرحلة الحالية solid عبر `solidStyle`، الباقي outline، أزرار الانتقال tonal عبر `tonalStyle`)، مؤشرات 4 بطاقات + `BaseProgressBar`، الاستهداف `BaseMultiSelect` للمناطق + segmented الجنس، جدول HTML للمستبينين، نافذة استيراد `BaseModal` (segmented المصدر + `<input type=file>` منسّق + `BaseTextarea` + عدّاد صفوف صالحة حيّ). **دروس:** ألوان الحالة `orange`/`surface-variant` تُطبَّع عبر `normVar` (→warning/on-surface)؛ حقل الاتحاد `gender` يتطلّب `as typeof`. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، **انتقال الحالة** (مسودة→نشط: الرقاقة والانتقالات المسموحة تحدّثت لـ«مغلق»)، **نافذة الاستيراد** (لصق CSV → «2 صفًا صالحًا» بتخطّي العنوان → استيراد → الجدول عرض سارة/محمد). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`UnifiedHubPage` (المركز الموحّد) Tailwind 100%** (`vuetifyInMain`=0) — 69 وسمًا. نُزع VMenu/VBtn/VCard/VSwitch/VIcon/VRow/VCol/VAvatar/VChip/VBadge/VBtnToggle/VExpandTransition/VTextField/VSelect/VDivider/VTooltip/VDialog/VCardTitle/VCardText/VCardActions. قائمة الإعدادات `BaseDropdown` (3 `BaseSwitch`)، بطاقة التعريف tonal، KPIs + بطاقات الأدوار شبكات، صندوق العمل: **رقائق-تبديل للأدوار/الحالة** (بديل VSelect multiple — تخزّن قيم الاتحاد الصحيحة بتسميات، بدل BaseMultiSelect الذي لا يفصل القيمة عن التسمية)، segmented التجميع + الكثافة، `BaseSelect` للفرز، `VExpandTransition`→`v-if`، طرق العرض المحفوظة رقائق مخصّصة، عناصر العمل بطاقات بأزرار قرار. نافذة حفظ العرض `BaseModal`. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، 4 KPIs + 6 عناصر، قائمة الإعدادات (3 مفاتيح أقسام)، فتح الفلاتر (بحث + فرز + رقائق أدوار/حالة + 4 أزرار تجميع)، **فلتر الحالة «يحتاج قرارك» → العناصر 6→4 والعدّاد تحدّث**. 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`PeerRequestsPage` (الطلبات المتبادلة) Tailwind 100%** (`vuetifyInMain`=0) — 49 وسمًا. نُزع VBtn/VChip/VTabs/VTab/VWindow/VWindowItem/VRow/VCol/VCard/VAvatar/VIcon/VDialog/VCardTitle/VCardText/VCardActions/VSelect/VTextarea/VCombobox/VAlert/VSnackbar. رقائق بدء سريع أزرار، تبويبان `.nav-tab` بـ`v-if`، بطاقات الطلبات بأزرار حسب الحالة، نافذة طلب جديد `BaseModal` (رقائق-تبديل النوع + `BaseSelect` الشخص + `BaseTextarea` + `BaseMultiSelect` المهارات من ALL_SKILLS + نصيحة AI tonal). يستهلك `EmptyState` المشترك. **درس:** النغمة الصلبة للرقاقة تتطلّب رمز ثيم صالح فيه `on-${color}` → `normColor` يطبّع orange/surface-variant/grey. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، التبويبان (واردة 2 / صادرة 3)، **دورة إرسال كاملة** (اختيار شخص + سبب → زر التفعيل → إرسال → التبويب صار «صادرة 4» + 4 بطاقات)، أزرار الإجراء في «الواردة» حسب الحالة (قبول/رفض/بدء التنفيذ). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`CompanyWishesPage` (إدارة رغبات الشركة) Tailwind 100%** (`vuetifyInMain`=0) — 47 وسمًا. نُزع VRow/VCol/VTabs/VTab/VWindow/VWindowItem/VCard/VTable/VChip/VTooltip/VBtn/VAvatar/VDialog/VCardTitle/VCardText/VCardActions/VTextField/VTextarea/VSnackbar. الإحصاء `StatCard` شبكة، تبويبان `.nav-tab`، جدول HTML للمرسلة (تلميحات عبر `title`)، بطاقات للمستلمة، 3 نوافذ `BaseModal` (تعديل/سحب/تفاوض). يستهلك `StatCard`+`EmptyState` المشتركين. **مُتحقَّق حيًّا** (دور company مؤقت ثم أُعيد): `vuetifyInMain=0`، تبويبان (مرسلة 3 جدول / مستلمة 2 بطاقات)، **نافذة التعديل** (3 حقول معبّأة + textarea)، **نافذة التفاوض** (المقابل معبّأ «3,500 ريال»). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`AssessmentsPage` (مركز التقييم) Tailwind 100%** (`vuetifyInMain`=0) — 42 وسمًا. نُزع VChip/VCard/VIcon/VRow/VCol/VAvatar/VBtn/VProgressLinear/VList/VListItem/VListItemTitle/VListItemSubtitle/VDivider/VDialog/VCardTitle/VCardText. شريط أنماط الأسئلة tonal، بطاقات الاختبارات المتاحة + التحديات (BaseProgressBar)، لوحة المتصدّرين بميداليات inline (ذهبي/فضي/برونزي خارج رموز الثيم)، قائمة المنجزة صفوف `border-t`، نافذة حجم الاختبار `BaseModal` بأزرار. يستهلك محرّك التلعيب الحقيقي. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، أنماط الأسئلة + 4 اختبارات + التحديات + المتصدّرين + 3 أشرطة تقدّم، **نافذة الحجم** (3 خيارات بعدد الأسئلة والزمن). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`AssessmentResultPage` (نتيجة الاختبار) Tailwind 100%** (`vuetifyInMain`=0) — 29 وسمًا. نُزع VCard/VProgressCircular/VChip/VRow/VCol/VIcon/VList/VListItem/VListItemTitle/VBtn/VSnackbar. حلقة النتيجة `BaseProgressRing`، بطاقتا القوة/التحسين، توصيات رقائق، 3 أزرار، حالة «غير متاحة». **مُتحقَّق حيًّا** (`?score=88&correct=22&total=25`): `vuetifyInMain=0`، حلقة 88% + المستوى + القوة/التحسين + التوصيات + «22 من 25»، **مشاركة النتيجة** (snackbar «تم نسخ نتيجتك»). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`AdminDashboardPage` (لوحة المدير) Tailwind 100%** (`vuetifyInMain`=0) — 27 وسمًا. نُزع VRow/VCol/VCard/VIcon/VChip/VAvatar/VBtn/VProgressLinear/VList/VListItem/VListItemTitle/VListItemSubtitle/VSnackbar. الإحصاء `StatCard` شبكة، طابور اعتماد الأدوار بصفوف وأزرار، توزيع المستخدمين `BaseProgressBar`، نشاطات المنصة صفوف، صحة النظام. **مُتحقَّق حيًّا** (دور admin مؤقت ثم أُعيد): `vuetifyInMain=0`، الأقسام الأربعة + 4 أشرطة توزيع، **قرار اعتماد** (الطابور 3→2). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`AssistantPage` (المساعد الذكي) Tailwind 100%** (`vuetifyInMain`=0) — 21 وسمًا. نُزع VCard/VBtn/VTextField/VList/VListItem/VAvatar/VIcon/VAlert/VChip/VProgressCircular/VDivider. شريط السجل الجانبي بأزرار `.conv-row` (helper scoped)، فقاعات المحادثة `bg-brand text-on-brand` (أنا)/`border-ui bg-surfalt` (AI)، بطاقة تحليل الملف المنظّمة، تنبيه استباقي tonal بحدّ `border-s-4` + إغلاق، مؤشّر الكتابة spinner CSS، اقتراحات `btn-tonal-brand`، حقل الإرسال `BaseInput` + زر إرسال في `#suffix` + زر إرفاق. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، السجل (محادثتان) + 6 اقتراحات، **دورة رسالة كاملة** (كتابة → فقاعة مستخدم + **ردّ AI** «سؤال جيد! بناءً على ملفك…»)، ونقر اقتراح يضيف فقاعة (مسار نظيف). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`SearchResultsPage` (نتائج البحث) Tailwind 100%** (`vuetifyInMain`=0) — 20 وسمًا. نُزع VBtn/VIcon/VChip/VAlert/VChipGroup/VCard/VList/VListItem/VListItemTitle/VListItemSubtitle/VDivider/VAvatar. زر حفظ البحث، عمليات محفوظة رقائق قابلة للحذف، نية AI + بدائل tonal، تبويبات النطاق رقائق-تبديل بعدّادات، نتائج بالتصنيف بطاقات صفوف، حالة فارغة `EmptyState`. **مُتحقَّق حيًّا** (`?q=Vue`): `vuetifyInMain=0`، العنوان + 3 تبويبات + نية AI + 19 نتيجة، **فلتر النطاق** (تبويب «الطلبات» → 19→5)، **زر الحفظ** (→ «محفوظ» + ظهور صفّ «محفوظة»). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`ApplicationsPage` (طلباتي) Tailwind 100%** (`vuetifyInMain`=0) — 18 وسمًا. نُزع VRow/VCol/VChip/VCard/VList/VListItem/VListItemTitle/VListItemSubtitle/VAvatar/VIcon/VMenu/VBtn/VDivider. الإحصاء `StatCard` شبكة، رقائق فلترة الحالة أزرار-تبديل، قائمة الطلبات صفوف `border-t`، قائمة إجراءات `BaseDropdown`، حالة فارغة `EmptyState`. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، 6 رقائق فلترة + الإحصاء، **فلتر «مقابلة»** (5→1)، **قائمة الإجراءات** (عرض الفرصة/سحب الطلب). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **`UnifiedAnalyticsPage` (التحليلات الموحّدة) Tailwind 100%** (`vuetifyInMain`=0) — 18 وسمًا. نُزع VBtn/VChip/VRow/VCol/VTextField/VSelect/VCard/VAvatar/VIcon. فلاتر المجالات أزرار-تبديل بلون كل مجال، شريط تحكم (بحث `BaseInput` + **أدوار رقائق-تبديل** بديل VSelect multiple + فرز `BaseSelect`)، شبكة مؤشرات بطاقات، حالة فارغة. **مُتحقَّق حيًّا:** `vuetifyInMain=0`، 5 فلاتر مجالات + 15 بطاقة، **فلتر «مالية»** (15→6). 0 أخطاء console. typecheck + **232 اختبار** + build خضراء.
  - [x] **تجربة المصادقة كاملةً** (`FormsLayout` + `LoginPage` + `RegisterPage`) صارت Tailwind. بدائيّان جديدان: `BaseInput` (تسمية + أيقونة بادئة + خانة لاحقة slot + رسالة خطأ + `inheritAttrs:false`) و`BaseCheckbox` (v-model منطقي **أو مصفوفة** مع `value`). `FormsLayout` (لوحة العلامة + لوحة النموذج) بلا Vuetify. **مُتحقَّق حيًّا (خرجت من الجلسة لأصلها):** الدخول (حقلان + إظهار/إخفاء كلمة المرور يعمل + خانة تذكّرني تبدّل + حلقة تركيز brand موجودة في CSSOM + رابط التسجيل)، التسجيل (3 بطاقات دور، المحدّد brand، تبديل الدور يغيّر خانات الأدوار الإضافية 2→1، 5 حقول). 0 أخطاء console. typecheck + 185 اختبار + build خضراء (الحزمة نزلت ~2KB). **درس تحقّق:** تحديثات Vue غير متزامنة — انتظر بعد النقر الاصطناعي قبل قراءة DOM/getComputedStyle (وإلا قراءة قديمة). **ملاحظة:** الباك-إند مطفأ + `USE_REAL_API=true` فالجلسة تُقرأ من localStorage بلا تحقّق خادمي؛ أعدت بناء authUser يدويًّا (roles تُملأ تلقائيًّا من `defaultRoleEntries`).
- [ ] نزع حزمة Vuetify بعد آخر صفحة
- [ ] تحقّق حيّ بعد كل مجموعة (لقطات + تباين)

## ⬜ المرحلة 6 — النشر: Docker + Nginx + staging + CI
- [ ] Dockerfile للواجهة (Vite build → nginx static)
- [ ] Dockerfile + compose للباك-إند (Node) + Postgres
- [ ] Nginx عكسي (واجهة `/` · API `/api`)
- [ ] CI: بناء + اختبار + نشر staging
- [ ] `docker compose up` يشغّل المكدّس كاملًا

---

**تشغيل الباك-إند محليًّا (بلا Docker):** `cd api && npm install && cp .env.example .env && npm run start:dev`
